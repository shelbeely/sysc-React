# Figlet Header
syscgo -effect beam-text -file /home/nomadx/Documents/sysc-Go/rama.txt -auto -display -theme nord -duration 5
echo "               //CHALLENGE THE iMPOSSIBLE//"
echo "═══════════════════════════════════════════════════════════════════"
# System Information using fastfetch (right-aligned)
fastfetch --logo none --config ~/.config/fastfetch/config.jsonc | grep -E "(DISTRO|DE/WM|SYSTEM|Linux|zsh|ghostty|Ryzer|GiB|hour)" | head -7

# To create the rama.txt file, run one of these commands:

# Option 1: Simple text
echo "RAMA" > /home/nomadx/Documents/sysc-Go/rama.txt

# Option 2: Fancy ASCII art (same as your figlet output)
figlet -f "DOS Rebel.flf" "RAMA" | sed '/^$/d' | head -7 > /home/nomadx/Documents/sysc-Go/rama.txt

# Notes about the flags:
# -auto     : Auto-size canvas to fit the text dimensions exactly
# -display  : Complete animation once and hold at final bright state (perfect for .zshrc)
# -duration : Set to 5 seconds to allow the full beam animation to complete once
